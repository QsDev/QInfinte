import { UI } from '../../../js/UI';
export declare namespace Services {
    class SearchServices extends UI.Navbar<UI.CItem> {
        constructor();
        initialize(): void;
        private OnItemSelectedDlgt;
        ApplyTo(s: UI.IService): void;
        private s;
    }
    class RFactureServices extends UI.Navbar<UI.CItem> {
        constructor();
        initialize(): void;
        private OnItemSelectedDlgt;
        private OnItemSelected(m);
        ApplyTo(s: UI.IService): void;
        private s;
    }
    class FactureService extends UI.Navbar<UI.CItem> {
        constructor();
        initialize(): void;
        private OnItemSelectedDlgt;
        ApplyTo(s: UI.IService): void;
        private s;
    }
    class FacturesService extends UI.Navbar<UI.CItem> {
        constructor();
        initialize(): void;
        private OnItemSelectedDlgt;
        ApplyTo(s: UI.IService): void;
        private s;
    }
    class MyClientsService extends UI.Navbar<UI.CItem> {
        constructor();
        initialize(): void;
        private OnItemSelectedDlgt;
        ApplyTo(s: UI.IService): void;
        private s;
    }
}
